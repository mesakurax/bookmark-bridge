{
  "name": "bookmark-bridge",
  "version": "2.1.0",
  "description": "在 Windows 上手动桥接 Chrome 与 Edge 的收藏夹、密码和浏览记录",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "check": "node --no-warnings --check bookmark-bridge.js && node --no-warnings --check history-sync.js && node --no-warnings --check password-migrate.js",
    "test": "node --no-warnings --test *.test.js"
  }
}
